apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: react-pvc
  namespace: gic-wesago
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 10Mi